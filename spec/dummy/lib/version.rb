# frozen_string_literal: true

module Dummy

  # WARNING: changing this version also requires changing the :version in version.json
  # changes should be added to :changes of version.json in the reverse order (latest top)
  VERSION = '0.1.1'

end
