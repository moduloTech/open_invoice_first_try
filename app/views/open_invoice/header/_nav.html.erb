<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to OpenInvoice.app_name, root_path, class: 'navbar-brand' %>

  <% if current_recipient %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarContent"
            aria-controls="navbarContent" aria-expanded="false" aria-label="<%= t('nav_toggle') %>">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item <%= active_class('open_invoice/invoices#index') %>">
          <%= link_to t('invoices.index.link'), auth_invoices_path, class: 'nav-link' %>
        </li>
      </ul>

      <div><%= current_recipient.email %></div>
    </div>
  <% end %>
</nav>
